# Loaded alongside application.yaml, so the main config is no longer shadowed by a
# same-named file earlier on the test classpath. Test overrides live in
# application-test.yaml and now overlay the real configuration instead of
# replacing it.
spring.profiles.active=test
